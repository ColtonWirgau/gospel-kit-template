// Neon PostgreSQL Client (Drizzle ORM)
//
// This file provides a type-safe database client for Neon PostgreSQL.
// Uncomment and configure when you're ready to use Neon for custom app data.

/*
import { drizzle } from 'drizzle-orm/neon-serverless';
import { Pool } from '@neondatabase/serverless';

// Create connection pool
const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
});

// Create Drizzle client
export const db = drizzle(pool);

// Example usage:
// import { db } from '@church/database/neon/client';
// import { prayerRequests } from '@church/database/neon/schemas/prayer-requests';
//
// const prayers = await db.select().from(prayerRequests).where(eq(prayerRequests.isApproved, true));
*/

// Placeholder export (remove when activating Neon)
export const db = null;
